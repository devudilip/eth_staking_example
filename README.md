# staking and unstaking tokens example

this app run towards Local Blockchain(Ganache)



Clone repo 

npm install


Run Ganache

truffle migrate(to deploy contracts to blockchain)

truffle exec scripts/issue_tokens.js (to issue tokens for stakers)
